name: Redeploy Stack
on: 
  repository_dispatch:
    types: [redeploy] 

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Log status
        run: echo "Zabbix reported a failure. Attempting to heal..."
